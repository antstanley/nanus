[package]
name = "nanus-adapter-local"
description = "The local adapters: a rooted filesystem, process-group shell execution, and the system clock."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
nanus-domain = { workspace = true }
nanus-ports = { workspace = true }
tokio = { workspace = true }
futures = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
ignore = { workspace = true }
globset = { workspace = true }
similar = { workspace = true }

[target.'cfg(unix)'.dependencies]
# Safe wrappers for `killpg`/`kill`, so the crate never needs `libc` and the
# workspace's `unsafe_code = "forbid"` stays intact.
nix = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
proptest = { workspace = true }

[lints]
workspace = true
